on:
  push:
    branches:
      - saurabh
  pull_request:
    branches:
      - saurabh
jobs:
  depchecktest:
    runs-on: label-1
    name: depecheck_test
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: install dependencies
        run: sudo npm install
      - name: Depcheck
        run: bash /mnt/sec_tools/dependency-check/bin/dependency-check.sh --scan ${{github.workspace}} --nvdApiKey 92d03fc0-5a98-46df-b9a8-6a8cee5971f1
        # uses: dependency-check/Dependency-Check_Action@main
        # id: Depcheck
        # with:
        #   project: 'wanderlust'
        #   path: '.'
        #   format: 'HTML'
          # out: 'reports' # this is the default, no need to specify unless you wish to override it
          # args: >
          #   --failOnCVSS 7
          #   --enableRetired
      - name: Upload Test results
        uses: actions/upload-artifact@master
        with:
           name: Depcheck report
           path: ${{github.workspace}}/reports

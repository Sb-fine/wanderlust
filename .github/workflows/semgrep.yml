on:
  push:
    branches:
      - saurabh
  pull_request:
    branches:
      - saurabh
jobs:
  semgrep:
    runs-on: label-1
    steps:
      - using: 'node20'
     # - uses: actions/checkout@v3
      - name: semgrep SAST
    
        run: cd /mnt/project/wanderlust
             semgrep ci

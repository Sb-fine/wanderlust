on:
  push:
    branches:
      - saurabh
  pull_request:
    branches:
      - saurabh
jobs:
  semgrep SAST:
    runs-on: label-1
    steps:
      - uses: actions/checkout@v3
      - name: semgrep SAST
        run: cd /mnt/project/wanderlust
             semgrep ci
